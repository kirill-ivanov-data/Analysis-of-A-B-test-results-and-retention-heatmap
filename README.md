# Аналіз результатів A/B-тестування та побудова retention heatmap
## A/B
Код: [ab-testin.ipynb](ab-testin.ipynb)

Результати тесту виявилися незадовільними та статистично незначущими. Хоча ключова метрика (ARPU) номінально зросла, статистичний аналіз показує, що це зростання є наслідком випадковості. Впровадження цього оновлення є ризикованим і може призвести до реальних фінансових втрат.

Детальніше: у тестовій групі статистично значущо впала конверсія в покупку (CR). Водночас середній дохід на платника (ARPPU) математично зріс, але розрахунки доводять, що це зростання зумовлене випадковими аномальними платежами, а не системним покращенням. Як наслідок, зміна нашої цільової метрики (ARPU) виявилася статистично незначущою.

**Висновок:** випускати оновлення недоцільно.

### План та логіка виконання:
1. Перед початком аналізу я вирішив зробити перевірку на SRM (Sample Ratio Mismatch), щоб впевнитися, що перекосу трафіку між групами немає. Робив я це за допомогою критерію Хі-квадрат, порівнявши фактичні розміри груп з очікуваною пропорцією розподілу. Різниця між групами виявилася статистично незначущою, що підтверджує коректність роботи системи сплітування.
2. Далі я розрахував p-value та довірчі інтервали для цільової метрики (ARPU) та проксі-метрик (CR і ARPPU). Декомпозицію я зробив для того, щоб у разі відсутності зростання ARPU можна було діагностувати, яка саме метрика дала збій.
    - CR (Конверсія). Для розрахунку конверсії я обрав Хі-квадрат, тому що це якісна (бінарна) метрика, а ми порівнюємо дві незалежні вибірки.
    - ARPPU та ARPU. Для них я застосував t-тест (для отримання p-value) та Бутстрап (для побудови довірчого інтервалу). t-тест був обраний тому, що, всупереч класичному уявленню про необхідність нормального розподілу самих даних, для нього насправді потрібен нормальний розподіл вибіркових середніх, що на наших обсягах даних виконується. Бутстрап для довірчого інтервалу я обрав через те, що метрики доходу є дуже асиметричними та мають викиди, тому класичні аналітичні формули для побудови інтервалів тут не підходять.

### Рішення:
Оскільки нашою єдиною цільовою метрикою є ARPU, а CR та ARPPU виступають лише проксі-метриками для пояснення її динаміки, поправки на множинну перевірку гіпотез не застосовувалися.
Статистичне обґрунтування:

- P-value для CR (Хі-квадрат): 0.0365 (статистично значуще падіння). Зниження частки платників не є випадковим.
- P-value ARPPU: 0.1002 статистично незначуще зростання.
- Довірчий інтервал (Confidence Interval) ARPPU: [-66.34, 730.65], надзвичайно широкий розкид. Зміна середнього чека може призвести як до зростання на 730 одиниць, так і до падіння на 66 одиниць на користувача.
- P-value ARPU: 0.5330 (зміна цільової метрики статистично незначуща).
- Довірчий інтервал (Confidence Interval) ARPU: [-3.04, 5.49]. Інтервал перетинає нуль, стійкого фінансового зростання не підтверджено.

##  Retention heatmap:
Розробив універсальну Python-функцію для когортного аналізу відтоку. Скрипт обробляє 2 датасети, сегментує користувачів за датою реєстрації, розраховує утримання для заданого періоду та візуалізує результати у вигляді теплової матриці (Heatmap).

Скріншот прикладу retention heatmap:
<img width="1533" height="848" alt="image" src="https://github.com/user-attachments/assets/c1f1b89e-348e-4153-aa57-a42f7e89938b" />
